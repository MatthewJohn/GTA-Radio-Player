
image: ubuntu:latest

before_script:
  - apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install --assume-yes g++ make autoconf qt5-qmake qt5-default qtmultimedia5-dev libqt5multimediawidgets5 libqt5multimedia5-plugins libqt5multimedia5

build:
  stage: build
  script:
    - qmake -makefile -o Makefile
    - make
  artifacts:
    paths:
      - gta-radio-player

sonarqube-check:
  image:
    name: sonarsource/sonar-scanner-cli:latest
    entrypoint: [""]
  variables:
    SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"  # Defines the location of the analysis task cache
    GIT_DEPTH: "0"  # Tells git to fetch all the branches of the project, required by the analysis task
  cache:
    key: "${CI_JOB_NAME}"
    paths:
      - .sonar/cache
  script:
    - sonar-scanner -Dsonar.qualitygate.wait=true -Dsonar.projectKey=gta-radio-player -Dsonar.sources=. -Dsonar.host.url=https://sonar.dockstudios.co.uk -Dsonar.login=cb9e8e14cd3f76947c4207ecafd445ef4ad9b185
  allow_failure: true
  only:
    - merge_requests
    - master
